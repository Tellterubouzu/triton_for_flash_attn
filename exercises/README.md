# Exercises

このディレクトリの exercise は `lessons/` と 1 対 1 に対応しています。

学習順序は次の通りです。

1. `docs/modules/NN_*.md` を読む。
2. `exercises/NN_*.md` を解く。
3. `lessons/NN_*.py` を実行して答え合わせする。

対応関係は `course_manifest.json` と `COURSE_MAP.md` に全件記載しています。未対応の exercise が混ざらないように、`scripts/validate_course_graph.py` で検査できます。
