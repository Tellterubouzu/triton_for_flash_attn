# Docs

このディレクトリの主経路は `docs/modules/` です。

各 module doc は必ず次の構成を持ちます。

1. その章で初めて重要になる用語の定義
2. 必要な数式や低レイヤー概念の説明
3. 対応する exercise へのリンク
4. exercise の答えとなる lesson へのリンク

`docs/reference/` は深掘り用の補足資料です。最初から全部読む必要はありません。`docs/glossary.md` は用語集です。
